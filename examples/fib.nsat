目标语言：Python

// 计算斐波那契数列第 n 项
将 [count] 初始化为 10

定义函数 [fib] 传入 [n]：
    如果 [n] 小于等于 [1]：
        返回 [n]
    否则：
        返回 [fib(n-1)] 加上 [fib(n-2)]

// 打印前 count 项
循环 [i] 从 [0] 到 [count]：
    输出 [fib(i)]

输出 "计算完成"